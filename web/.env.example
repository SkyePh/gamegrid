# API base URL for search/validate (baked in at build time).
# Dev: omit to use http://localhost:3001
# Prod (GitHub Pages): set before `npm run build`, e.g.
# VITE_API_URL=https://gamegrid-6pcg.onrender.com
